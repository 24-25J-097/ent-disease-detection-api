export enum EmailType {
    WELCOME = 0,
    CREATE_USER = 1,
    DEACTIVATE_USER = 2,
    NOTIFICATION = 3,
    CONTACT_US = 4,
}

export enum APIOperation {
    READ = 0,
    CREATE = 1,
    UPDATE = 2,
    DELETE = 3,
}
